{
  "name": "capauth-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "CapAuth browser extension — one-click passwordless PGP login",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.js",
    "build:firefox": "node scripts/build.js --firefox",
    "build:icons": "node scripts/generate-icons.js",
    "watch": "node scripts/build.js --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist/chrome && zip -r ../../capauth-extension.zip .",
    "package:firefox": "npm run build:firefox && cd dist/firefox && zip -r ../../capauth-extension-firefox.xpi ."
  },
  "dependencies": {
    "openpgp": "^5.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "happy-dom": "^15.11.7",
    "vitest": "^2.1.9"
  }
}
